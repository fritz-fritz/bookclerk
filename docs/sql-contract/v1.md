# Bookclerk SQL contract v1

This document is the **normative** definition of `sqlContractVersion = 1`.
Adapter admission is “passes Bookclerk SQL v1 conformance,” not “looks like
SQLite or PostgreSQL.” Machine-readable vectors live under
[`crates/bookclerk-db-exec/testdata/sql_v1/`](../../crates/bookclerk-db-exec/testdata/sql_v1/)
and are consumed by `bookclerk-db-exec` lowering tests and library conformance.

## Version compatibility

- Contract versions are **monotonic supersets**: every statement form, type,
  and semantic guarantee in version *N* remains valid in version *N+1*.
- Guests advertise the highest version they implement as `sqlContractVersion`.
- Hosts require `sqlContractVersion >= SQL_CONTRACT_VERSION` (currently `1`).
- A **non-superset** change must introduce a new major contract version and a
  new host minimum; until then, do not weaken `>=` into a range.

## Grammar (canonical form)

Guest SQL is admitted in this order:

1. **SQL-v1 grammar** — positive/versioned Bookclerk SQL (not a SQLite dialect).
2. **Binding scope/security** — isolated schema, reserved-name, and ref checks.
3. **Capability/limit validation** — binds, result selection, negotiated caps.
4. **Canonical `ExecuteRequest`** — host wrap (receipts) without engine identity.
5. **Adapter-edge lowering** — at execute, against the isolated physical DB.

Hosts emit **canonical Bookclerk SQL**. Adapters lower to the engine at execute
time. Domain planners must not branch on engine identity.

- **DML and queries** — `bookclerk-db-exec::lower_canonical_sql` (placeholders,
  `IFNULL`, `json_extract`, `INSERT OR IGNORE`, 2+-arg `min`/`max`, `json_valid`).
- **DDL types and identity** — adapter execution edge
  (`schema_sql_for_backend` for host schema packs,
  `lower_binding_ddl_execute_request` for plugin-owned `CREATE`/`DROP`).
  The generic DML pass does not classify statements as DDL.

### Placeholders

- Positional binds use bare `?` only (1-based order of appearance in code spans).
- `$n`, `?NNN`, and other engine-specific bind spellings are outside SQL v1
  and are rejected for every guest before adapter dispatch.
- Placeholders inside string/identifier literals, line/block comments, and
  PostgreSQL dollar-quotes are not binds (adapters must preserve them).

### Statement forms

Supported top-level forms for guest-authorized and host-authored plans:

| Form | Notes |
| --- | --- |
| `SELECT …` | Including `WITH` CTEs that do not nest DML. Mixed aggregate + non-aggregate select lists are SQLite-shaped and **not** in this contract; keep aggregates in their own statement (or `GROUP BY`). |
| `INSERT INTO …` / `INSERT OR IGNORE INTO …` | Optional `RETURNING` |
| `UPDATE …` / `DELETE …` | Optional `RETURNING` |
| Schema DDL | Host-only for library and session guests. `GuestSqlPolicy::binding_owned` admits idempotent `CREATE`/`DROP` `TABLE`/`INDEX` `IF [NOT] EXISTS` on a physically separate plugin database. Binding helpers are this contract (not a wider SQLite dialect). Postgres adapters lower canonical types (`INTEGER PRIMARY KEY AUTOINCREMENT` → `BIGSERIAL`, `BLOB` → `BYTEA`, case-insensitive) at their execute edge. |

`INSERT OR IGNORE` is canonical; Postgres adapters lower to
`INSERT … ON CONFLICT DO NOTHING` (conflict clause before `RETURNING`).
`REPLACE INTO`, `INSERT OR REPLACE` / `OR ABORT` / `OR FAIL` / `OR ROLLBACK`,
`GLOB`, `MATCH`, `REGEXP`, and `COLLATE` (including `COLLATE NOCASE`) are
outside SQL v1 and are rejected before adapter dispatch.

Binding `CREATE TABLE` / `CREATE INDEX` types are `INTEGER`, `REAL`, `TEXT`,
`BLOB`, and `BOOLEAN` only (plus the proven `INTEGER PRIMARY KEY AUTOINCREMENT`
identity spelling). `BOOL` is not an alias. Tails such as `STRICT`,
`WITHOUT ROWID`, `JSONB`, `NUMERIC(p,s)`, `VARCHAR(n)`, `CASCADE` / `RESTRICT`
on `DROP`, and backend-specific index clauses (`USING`, `INCLUDE`,
column `COLLATE`) are rejected. Adapter lowering of type/identity tokens and
`RETURNING` is case-insensitive in code spans (the host write-gate constant
is still matched exactly).

### Identifiers and quoting

- Unquoted identifiers fold as the guest SQL authorization layer defines
  (`guest_sql`); adapters must not reinterpret authorization.
- Double-quoted identifiers are preserved through lowering.

## Types and `DbValue`

| Wire (`DbValue`) | Canonical SQL expectation |
| --- | --- |
| `null(expectedType)` | SQL `NULL` with typed metadata |
| `bool` | Canonical SQL `BOOLEAN` (not INTEGER 0/1 storage leaking onto the wire) |
| `int64` | Exact 64-bit integer (not JSON number) |
| `float64` | IEEE float |
| `text` | UTF-8 text |
| `bytes` | Opaque byte string |

Unknown Cap'n union members fail closed as `unsupported`.

### NULL and coercion

- Typed null carries `expectedType` for empty-result metadata and decode.
- Adapters must not collapse distinct Cap'n types through JSON number/array
  shims on the granted Cap'n path.
- Computed numerics that would be engine `NUMERIC` (Postgres `sum` of
  `BIGINT`, `avg`, `round` of an untyped literal) should `CAST` to
  `INTEGER` or `REAL` so the cell is in the universal `DbValue` domain.
- Conformance vectors compare **typed** `DbValue` cells (`Int64` is not
  `Float64(n)` or `Text("n")`). SQLite expression columns may still be
  `DbType::Unspecified`; Postgres OID mapping should be `Int64` / `Text`.

## Helpers (canonical)

| Canonical | Meaning |
| --- | --- |
| `IFNULL(a, b)` | First non-null |
| `json_extract(col, '$.path')` | JSON path extract (text) |
| `json_object(k, v, …)` | Object constructor |
| `json_valid(expr)` | Predicate (`= 0` / `= 1`) or integer 0/1 |
| `min(a, b, …)` / `max(a, b, …)` | Scalar least/greatest (2+ args). One-arg `MIN`/`MAX` stay aggregates. |

Postgres lowering maps these to `COALESCE`, `jsonb` operators,
`json_build_object`, `IS [NOT] JSON`, and `LEAST`/`GREATEST` respectively. SQLite/D1
execute the canonical forms directly. `MAX(attempt_count, 1)` in host plans is the
same 2-arg scalar form.

Plugin-owned database bindings (`GuestSqlPolicy::binding_owned`) authorize
these helpers plus portable scalars (`abs`, `avg`, `cast`, `coalesce`, `count`,
`length`, `lower`, `min`, `nullif`, `replace`, `round`, `substr`, `sum`,
`trim`, `upper`). `hex` is not portable (PostgreSQL has no `hex()`); it remains
available to library guests. SQLite-only names (`strftime`, `typeof`, `group_concat`,
`iif`, `instr`, `quote`, `total`, `date`, `datetime`, `time`, `json_array`)
are denied so a binding cannot depend on a dialect the contract does not
guarantee. See [`docs/plugins.md`](../plugins.md#isolated-plugin-database-bindings).

## Result semantics

- **`rowsAffected`**: mandatory (`affectedRows` capability). Reports rows
  changed by DML; `INSERT OR IGNORE` / `ON CONFLICT DO NOTHING` that insert
  zero report `0`.
- **`RETURNING`**: mandatory (`returning` capability). Column order and names
  come from engine metadata; empty results still expose column metadata.
- **Transactions**: one `ExecuteRequest` is one atomic batch. Failure before
  commit rolls back all statements in the request.
- **Errors**: stable categories (`conflict`, `deadline`, `invalidParams`,
  `internal`, …). Ambiguous commit / lost response uses durable receipt replay
  of the original `ExecuteReply`.
- **Bounds**: `maxResultRows`, `maxResultBytes`, `maxAtomicResultBytes`,
  `maxRequestBytes` fail closed before commit / dispatch.

## Conformance admission

A new SQL adapter is admitted when it:

1. Negotiates `DbCapabilities` with `sqlContractVersion >= 1` and required
   semantic flags/limits (not by matching a plugin id or `sqlFamily`).
2. Executes the **sql_v1** corpus (lowering + shared behavioral vectors) without
   host library changes keyed on adapter identity.
3. Keeps `sqlFamily` / `dialect` on `DbBootstrap` (host-private bootstrap
   metadata) only — not on typed `DbCapabilities` (`abiMinor` 13 tombstones
   `obsoleteSqlFamily` / `obsoleteDiagnosticEngine`). Architecture lint
   forbids planner reads of bootstrap fields.
