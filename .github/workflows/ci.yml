name: CI

on:
  push:
    branches: [main]
  pull_request:

# Least privilege: checkout + local cargo build/test only.
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"
  RUSTFLAGS: "-D warnings"

jobs:
  check:
    name: fmt / clippy / test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          env-vars: RUSTFLAGS
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Build UI
        run: |
          cd ui
          npm ci
          npm run build

      - name: Format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Plugin host without in-process sources
        run: cargo check -p bookclerk-plugin --no-default-features

      - name: Build first-party plugin artifacts
        run: |
          cargo build -p bookclerk-plugin-echo-integration \
            -p bookclerk-plugin-source-audible \
            -p bookclerk-plugin-source-libro \
            -p bookclerk-plugin-source-chirp \
            -p bookclerk-plugin-source-graphicaudio \
            -p bookclerk-plugin-integration-audiobookshelf
          ./scripts/stage-first-party-plugins.sh debug "$RUNNER_TEMP/bookclerk-plugins"

      - name: Test
        env:
          BOOKCLERK_PLUGIN_ARTIFACTS: ${{ runner.temp }}/bookclerk-plugins
        run: cargo test --workspace

  release:
    name: release build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          env-vars: RUSTFLAGS
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Build UI
        run: |
          cd ui
          npm ci
          npm run build

      # bookclerk-media-worker ships with the hosts, not alongside them as an
      # extra: both binaries refuse media jobs when it is not installed.
      - name: Build release binaries
        env:
          BOOKCLERK_DIAGNOSTICS_COLLECTOR_URL: ${{ vars.DIAGNOSTICS_COLLECTOR_BASE_URL }}
        run: cargo build --release -p bookclerk-cli -p bookclerkd -p bookclerk-media-worker

      - name: Media worker is present beside the hosts
        run: test -x target/release/bookclerk-media-worker

  # The confinement backends are entirely per-OS, so an ubuntu-only run says
  # nothing about Seatbelt or the Windows path. This also exercises the media
  # worker's negative tests, which are what prove a job cannot reach anything
  # it did not declare.
  confinement:
    name: sandbox + media worker (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          env-vars: RUSTFLAGS
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Clippy
        run: >
          cargo clippy -p bookclerk-sandbox -p bookclerk-media
          -p bookclerk-media-worker --all-targets -- -D warnings

      - name: Test
        run: cargo test -p bookclerk-sandbox -p bookclerk-media -p bookclerk-media-worker

  # Explicit tray matrix so each OS backend is compiled (ksni on Linux,
  # tray-icon without GTK features on Windows/macOS).
  tray:
    name: tray (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          env-vars: RUSTFLAGS
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Clippy bookclerk-tray
        run: cargo clippy -p bookclerk-tray --all-targets -- -D warnings

      - name: Test bookclerk-tray
        run: cargo test -p bookclerk-tray
