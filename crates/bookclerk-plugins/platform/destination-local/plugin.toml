api_version = 3
id = "local"
name = "Local output"
version = "0.1.0"
logo = "assets/logo.svg"
runtime = "native"
command = "./bookclerk-plugin-destination-local"
entrypoints = ["storage"]

[capabilities.network]
mode = "deny"

[vars]

[work_fs]
