{
  "name": "libation-diagnostics",
  "private": true,
  "description": "Libation diagnostics Worker (Cloudflare Builds)",
  "type": "module",
  "scripts": {
    "deploy": "bash scripts/deploy-with-secrets.sh",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
