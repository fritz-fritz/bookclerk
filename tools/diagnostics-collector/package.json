{
  "name": "bookclerk-diagnostics",
  "private": true,
  "description": "Bookclerk diagnostics Worker (Cloudflare Builds)",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test version-filter.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
