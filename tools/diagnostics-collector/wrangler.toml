# Cloudflare Worker published via Cloudflare Builds.
# Dashboard → Workers → Connect repo → Root directory: tools/diagnostics-collector
name = "libation-diagnostics"
main = "worker.js"
compatibility_date = "2026-01-01"
workers_dev = true

# Non-secret config (optional overrides):
# [vars]
# B2_ENDPOINT = "https://api.backblazeb2.com"

# Secrets (Cloudflare Builds / dashboard — never commit):
#   B2_KEY_ID
#   B2_APPLICATION_KEY
#   B2_BUCKET_ID
#   REPORT_API_KEY          # GitHub Action → GET /report
#   CLIENT_IP_HASH_SALT     # optional enrichment
