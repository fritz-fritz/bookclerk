# OSV-Scanner config for the root Cargo.lock.
# https://google.github.io/osv-scanner/configuration/

[[IgnoredVulns]]
id = "RUSTSEC-2023-0071"
# Re-evaluate when RustCrypto/RSA ships a constant-time fix or audible-rs
# drops/replaces the rsa dependency.
ignoreUntil = 2026-12-31
reason = """
Marvin Attack timing sidechannel in the rsa crate; no patched release exists
(RUSTSEC patched = []). Pulled transitively via audible-rs (Audible auth).
Bookclerk is a local CLI/daemon: RSA private-key ops are not exposed as a
network service where remote attackers can observe response timing.
"""
